# 安全に使うための注意

## 共有しないもの

パスワード、APIキー、秘密鍵、ログイン状態を含むファイル、顧客名簿、住所、電話番号、注文データ、未公開の商品情報、契約書は、この共有リポジトリへ保存しません。非公開リポジトリも共同の閲覧先です。

個別業務のファイルと生成物は local/ または別の業務フォルダへ置いてください。local/ は .gitignore の対象ですが、暗号化、AIへの送信防止、OSのクラウド同期防止の機能ではありません。機密データをAIへ添付する前に送信範囲と利用権限を確認します。

## AIが確認する行為

メール・LINE送信、SNSやページの公開、Notion等への保存、広告費の発生、購入、削除、本番変更は、内容と対象を確かめてから承認します。スキルの「自動化」や「実行」という記述だけを送信・支出の許可にしません。

接続が必要になったときは正規の認証画面を使い、APIキーをチャットに貼り付けないでください。必要最小限の権限を選びます。

## 事故に気づいたら

1. そのファイルの送信・共有・同期を止めます。疑わしい値をIssueやチャットへ貼り付けません。
2. 認証情報を漏らした可能性があれば、正規の管理画面で失効・再発行し、アクセス履歴を確認します。
3. リポジトリ管理者へ、値を含めずに影響するファイル名・時刻・共有範囲を連絡します。
4. ファイル削除だけでは過去のGit履歴や既に取得されたコピーは消えません。履歴対応は管理者が範囲を確認して行います。

## 点検の限界

同梱のチェックは特定パターン、ファイル構成、リンク、ハッシュの検査です。すべての個人名、文脈上の機密、最新法令への適合を自動判定しません。配布更新には人による内容確認も必要です。
GitフックやGitHub Actions、外部へのログ送信は自動設定しません。
